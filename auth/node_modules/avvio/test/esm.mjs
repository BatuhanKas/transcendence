import { test } from 'tap'
import boot from 'avvio'

test('support import', async (t) => {
  const app = boot()

  app.use(import('avvio/test/fixtures/esm.mjs'))

  await app.ready()

  t.equal(app.loaded, true)
})
