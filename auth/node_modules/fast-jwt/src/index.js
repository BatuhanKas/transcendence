'use strict'

const { TokenError, TOKEN_ERROR_CODES } = require('fast-jwt/src/error')
const createDecoder = require('fast-jwt/src/decoder')
const createVerifier = require('fast-jwt/src/verifier')
const createSigner = require('fast-jwt/src/signer')

module.exports = {
  TokenError,
  TOKEN_ERROR_CODES,
  createDecoder,
  createVerifier,
  createSigner
}
